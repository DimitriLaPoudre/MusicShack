services:
  # client_web:
  #   build: ./client_web
  #   restart: unless-stopped
  #   user: "1000:1000"
  #   ports:
  #     - "5173:5173"
  #   environment:
  #     BACKEND_URL: ${BACKEND_URL}

  server:
    build: ./server
    restart: unless-stopped
    user: "1000:1000"
    ports:
      - "8080:8080"
    volumes:
      - ./server/db:/db
      - ./users:/users
    environment:
      API_URL: "https://tidal.401658.xyz/"
      PORT: 8080
      JWT_SECRET: ${JWT_SECRET}

